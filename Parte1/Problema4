''' 
Estructuras en Python:
Escribir un programa que permita representar puntos en el plano 
y además implemente una función para calcular la distancia entre dos puntos.
'''

import math

# Clase para representar un punto en el plano
class Punto:
    def __init__(self, x, y):
        self.x = x  # Coordenada x
        self.y = y  # Coordenada y
    
    def __str__(self):
        return f"({self.x}, {self.y})"
    
    # Método para calcular la distancia entre dos puntos
    def distancia(self, punto2):
        # Fórmula de la distancia euclidiana
        return math.sqrt((self.x - punto2.x)**2 + (self.y - punto2.y)**2)

# Crear dos puntos
p1 = Punto(1, 2)
p2 = Punto(4, 6)

# Imprimir los puntos
print(f"Punto 1: {p1}")
print(f"Punto 2: {p2}")

# Calcular y mostrar la distancia entre los puntos
dist = p1.distancia(p2)
print(f"La distancia entre los puntos es: {dist:.2f}")

'''
En Python, no existe un tipo directo equivalente a las estructuras (struct) 
de C++, pero las clases pueden usarse de manera similar. 
'''