''' Escribir un programa que permita representar puntos en el plano 
y además implemente una función para calcular la distancia entre dos puntos.'''

import math

def crear_punto(x, y):
    return {'x': x, 'y': y}  # Estructura del punto

def calcular_distancia(punto1, punto2):
    dx = punto2['x'] - punto1['x']
    dy = punto2['y'] - punto1['y']
    return math.sqrt(dx**2 + dy**2)

# Ejemplo de uso
x1, y1 = map(float, input("Introduce las coordenadas del primer punto (x y): ").split())
x2, y2 = map(float, input("Introduce las coordenadas del segundo punto (x y): ").split())

# Creación de puntos
punto1 = crear_punto(x1, y1)
punto2 = crear_punto(x2, y2)

# Cálculo de distancia
distancia = calcular_distancia(punto1, punto2)

# Resultado
print(f"La distancia entre los puntos {punto1} y {punto2} es: {distancia:.2f}")
