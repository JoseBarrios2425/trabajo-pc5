'''
Archivos en Python: 
Implemente un programa usando funciones para escribir datos en un archivo de texto
'''

def crear_archivo(nombre_archivo):
    with open(nombre_archivo, 'w') as archivo:  # 'w' crea o sobreescribe el archivo
        pass  

def escribir_datos(nombre_archivo, datos):
    with open(nombre_archivo, 'a') as archivo:  # 'a' abre el archivo para añadir contenido
        for dato in datos:
            archivo.write(dato + '\n')  # Escribe cada dato en una línea nueva

# Nombre del archivo a crear
nombre_archivo = "datos.txt"
# Crear el archivo
crear_archivo(nombre_archivo)

# Datos a escribir en el archivo
datos_a_escribir = [
    "Hola mundo.",
    "Código escrito en python"
]

# Escribir los datos en el archivo
escribir_datos(nombre_archivo, datos_a_escribir)

print(f"Se ha creado el archivo '{nombre_archivo}' y se han escrito los datos.")

'''
Python tiene una sintaxis más simple y directa para trabajar con archivos. Usamos 
with open(...) para abrir y trabajar con archivos, lo cual automáticamente maneja el cierre 
del archivo. Esto hace que el código sea más limpio y seguro.
En C++, el manejo de archivos se hace a través de flujos de entrada y 
salida (ifstream, ofstream, fstream). Para abrir y escribir en un archivo, necesitas 
crear un objeto de flujo y luego abrir el archivo explícitamente.
'''